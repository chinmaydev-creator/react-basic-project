pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building python main file.'
                sh 'python main.py'
            }
        }
    }
}